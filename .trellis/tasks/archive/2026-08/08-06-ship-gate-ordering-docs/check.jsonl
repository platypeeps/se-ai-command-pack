{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "Acceptance criteria are checked against the conventions recorded here; confirm both gates are named with their firing conditions and the ordering that avoids them, discoverable before they fire rather than after. For pack.review-scope that means all three scope categories from the check's own predicates -- not only copied/generated files -- plus the three accepted headings, which are a separate set, and the ordering consequence that planning finalization adds the journal/index category after the PR body was authored."}
