# Harden the se-review-skills inventory analyzer

## Goal

Review snapshot 726d2d57c275b1d600940fbd523528081a43a898bfdaac2cdad34dec06fba0ae; findings 5.7.9.1 through 5.7.9.4. Restore Python runtime compatibility or controlled fallback, report test-text references honestly, mark unresolved installed copies non-changeable, and exclude generated bytecode from snapshots. Affected template skill, analyzer, and focused tests under templates/skills/se-review-skills and tests/test_skill_review.py.

## Requirements

- Make the required analyzer path work on the repository's documented runtime,
  or fail with an explicit minimum-version message and controlled manual-
  coverage fallback. Do not silently lose deterministic inventory coverage.
- Report top-level substring matches conservatively as test-text references;
  reserve behavioral pin terminology for semantically verified assertions.
- Mark an installed skill changeable only when ownership is verified, an
  allowed canonical template root exists, and the source resolves inside it.
  Unowned copies must remain reviewable evidence but non-changeable.
- Exclude ignored directories and generated bytecode such as `__pycache__` and
  `*.pyc` from related-template inventory and snapshot hashing.
- Preserve bounded-root, symlink, ownership, installed-copy, canonical-
  allowlist, drift, and selector safety behavior.
- Update the analyzer schema/version if a public field is renamed, and update
  all first-party consumers and tests in the same task.
- Change canonical skill/analyzer content only under
  `templates/skills/se-review-skills/**`; tests may establish the contract but
  are not remediation targets.

## Acceptance Criteria

- [ ] Python 3.9 receives either a successful compatible inventory or a concise
      prerequisite/fallback result; Python 3.10+ inventory remains successful.
- [ ] Incidental test-name mentions are not represented as proven behavioral
      pins, and scanner limits are disclosed.
- [ ] An unresolved installed skill has `changeable=false` and
      `taskRouting.canCreateTask=false`.
- [ ] Changing only ignored bytecode leaves related resources and snapshot IDs
      unchanged.
- [ ] Existing inventory, ownership, path-boundary, installed-drift, and
      selector tests remain green.
- [ ] Focused analyzer tests, all skill contract tests, generated target checks,
      and the repository quality gate pass.

## Notes

- Findings: `5.7.9.1`, `5.7.9.2`, `5.7.9.3`, and `5.7.9.4`; routing state:
  `untracked`.
- Treat this as a complex planning task: add `design.md` and `implement.md`
  before starting implementation.
- Primary regression risks are snapshot churn, schema-consumer breakage, and
  accidentally enabling mutation of unowned installed copies.
