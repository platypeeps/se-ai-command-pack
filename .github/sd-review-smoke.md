# sd-github-review rollout smoke

Temporary. This pull request exists only to give the installed routed-review
lane a head to write a receipt against, proving the lane actually runs after the
v0.5.0 rollout. It is closed without merging once the
`sd-github-review/receipt` check run is recorded.
