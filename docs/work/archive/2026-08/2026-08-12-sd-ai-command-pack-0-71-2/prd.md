---
title: Refresh SD AI command pack to 0.71.2
status: done
created: 2026-08-12
branch: chore/sd-ai-command-pack-0.71.2
---
# Refresh SD AI command pack to 0.71.2

## Goal

Move this repository's installed SD AI command pack from 0.71.1 to 0.71.2. A
managed dependency refresh driven by the fleet campaign, not a product change.

## Release identity

Immutable and verified by fleet preflight before this task was created:

- release: `0.71.2`
- tag: `v0.71.2` at `4663d5cdc78d2c6d11aaeb839ab79ec6fe2a1afe`
- payload: `sha256:9770ef5c125068381c7d60f0137aa11a7c879dbcb020f110201712a42575e9aa`
- platforms: `claude`, `gemini`, `github`, `opencode`
- base commit: `6e0d657b34cfae00f8d524a81fbdbe8efc352fff`
- campaign: `refresh-0.71.2-20260813T045840Z-c7`

## Managed scope

Installer-managed surfaces only. No product code:

- pack surfaces written by `install.py` for the four platforms above;
- `.sd-ai-command-pack/` receipts and provenance;
- the pack-managed `.obsidian-kb` block in `.gitignore`;
- this task's own Trellis artifacts.

This repository declares no candidate preparation commands in the fleet
manifest, so there is no regenerated map to commit.

## Requirements

1. Install 0.71.2 for exactly the four platforms above, using only the command
   printed by fleet preflight.
2. Pass the install audit for those same four expected platforms.
3. Pass the manifest-ordered candidate check: `bash
   scripts/sd-ai-command-pack-housekeeping.sh --self-test`.
4. Pass the repository's documented full local gate.
5. Publish one completion bundle whose work commit carries every managed
   change, so no follow-up commit and no force push is needed.

## Constraints

- No product-code edits. A diff outside the managed scope above is a failure of
  this task, not something to accommodate.
- Never stash, reset, or clean to make a stage pass.
- This repository is itself an AI command pack. Do not let its own pack-shaped
  files be confused with the installed `sd-ai-command-pack` surfaces; only the
  latter are in scope.

## Acceptance Criteria

- [ ] Installed provenance reports `0.71.2`.
- [ ] Install audit passes for `claude`, `gemini`, `github`, `opencode`.
- [ ] The housekeeping self-test passes.
- [ ] The full local gate passes.
- [ ] The pushed head carries one work commit and the completion receipt
      validates.
- [ ] The PR merges and post-merge verification confirms `0.71.2` on `main`.

## Finish-work expectation

`sd-ai-command-pack-fleet-publish.py` folds the work commit, the task archive,
and the journal tail into the reviewed head as a single completion bundle. The
pushed head already carries all bookkeeping; the merge gate must find a clean
tree.

## Verification

Every criterion is checked by a command in this lane — preflight, install audit,
the candidate self-test, the local gate, the publish receipt, and the post-merge
audit. None depends on judgment.

This repository's `.gitignore` already carries the current
`# Generated by sd-ai-command-pack.` marker, so the pack-side ignore-block
refresh must run and report `refreshed` while producing no `.gitignore` hunk in
the work commit.
